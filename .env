# Copy this file to .env and adjust as needed

# Local development (run services on your machine)
POSTGRES_URL=postgres://postgres:postgres@localhost:5432/notifications
REDIS_URL=redis://localhost:6379/0
RABBIT_URL=amqp://guest:guest@localhost:5672

# API
APP_PORT=8080
LOG_LEVEL=info

# Outbox publisher toggle (runs inside API process if set to 1)
RUN_PUBLISHER=1

# Optional: JWT public key (base64) if you add auth later
# JWT_PUBLIC_KEY_BASE64=

# --- Docker Compose defaults (uncomment when running inside compose) ---
POSTGRES_URL=postgres://postgres:postgres@postgres:5432/notifications
REDIS_URL=redis://redis:6379/0
RABBIT_URL=amqp://guest:guest@rabbitmq:5672